import 'package:flutter/material.dart';

/// Stores default [UserData] member values in case some data is missing on the
/// Database.
class DefaultUserData {
  static const Color defaultColor = Color(0xFFEED6D6);
}
